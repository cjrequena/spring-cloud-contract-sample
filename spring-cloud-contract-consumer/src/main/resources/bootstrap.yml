server:
  port: ${SERVER_PORT:9080}
  contextPath: /
management:
  context-path: /admin # Management endpoint context-path. For instance `/actuator`
  security.enabled: false
---
spring:
  application:
    name: spring-cloud-contract-consumer-sample
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}