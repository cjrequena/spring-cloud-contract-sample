server:
  port: ${SERVER_PORT:8080}
  contextPath: /
management:
  context-path: /admin # Management endpoint context-path. For instance `/actuator`
  security.enabled: false
---
spring:
  application:
    name: spring-cloud-contract-producer-sample
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
